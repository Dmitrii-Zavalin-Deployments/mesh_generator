{
  "$id": "mesh_generator_config_schema.json",
  "title": "Mesh Generator Configuration Schema",
  "type": "object",
  "description": "External configuration parameters used by the Mesh Generator to produce the CFD-ready grid and solid/fluid/wall mask.",
  "additionalProperties": true,
  "properties": {
    "tolerance": {
      "type": "number",
      "description": "Geometric tolerance used during STEP parsing, surface reconstruction, and point-in-solid classification."
    },
    "max_element_size": {
      "type": "number",
      "description": "Maximum allowed mesh element size used when computing grid resolution."
    },
    "min_element_size": {
      "type": "number",
      "description": "Minimum allowed mesh element size used when computing grid resolution."
    }
  },
  "required": [
    "tolerance",
    "max_element_size",
    "min_element_size"
  ]
}